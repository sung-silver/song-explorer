function Song({title, no, singer}) {
    return (
        <div>
            <div>{title}</div>
            <div>{no}</div>
            <div>{singer}</div>
        </div>
    );
}

export default Song;